services:
  - type: web
    name: equisproject-online
    runtime: node
    buildCommand: bun install && bun run build
    startCommand: bun run apps/api/src/index.ts
    envVars:
      - key: NODE_ENV
        value: production
